import XCTest
@testable import mux_data_container

final class mux_data_containerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(mux_data_container().text, "Hello, World!")
    }
}
